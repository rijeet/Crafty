@model IEnumerable<Crafty.Models.Cart_tbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Total_Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product_tbl.Product_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User_tbl.Image)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Total_Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product_tbl.Product_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User_tbl.Image)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cart_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cart_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cart_ID })
        </td>
    </tr>
}

</table>
